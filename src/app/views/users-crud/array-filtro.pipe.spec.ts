import { ArrayFiltroPipe } from './array-filtro.pipe';

describe('ArrayFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
